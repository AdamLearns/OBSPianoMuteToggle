OBS_HOST=localhost
OBS_PORT=4455
OBS_PASSWORD=your_password_here
OBS_INPUT_NAME=Mic/Aux

# leave blank to auto-select first available port
MIDI_PORT=
CHORD_WINDOW_MS=200     # how long (ms) to wait for all 3 notes to arrive
COOLDOWN_MS=1000        # minimum ms between successive triggers
